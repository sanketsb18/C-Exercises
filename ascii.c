#include<stdio.h>

int main()
    {
        char c;

        printf("Enter the character:");
        scanf("%c", &c);
        printf("ASCII value of character %c = %d\n", c, c);
    
        return 0;
}